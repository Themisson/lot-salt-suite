#include "salt/SaltWallStressDiagnostics.hpp"

namespace lss::salt {

static_assert(sizeof(SaltWallStressSample) > 0);
static_assert(sizeof(SaltWallStressDiagnostics) > 0);

}  // namespace lss::salt
