# Phase 11.10Z fixture.
# Diagnostic enabled without initial sigma_theta must block, not dispatch.
fixture:
  id: diagnostic_missing_sigmatheta_blocks
  phase: "11.10Z"
  scenario: missing_sigmatheta_blocks
  expected:
    diagnostics_enabled: true
    diagnostic_output_expected: true
    selected_model: PKN
    gate_status: FRACTURE_GATE_BLOCKED_SIGMATHETA_INITIAL_STATE
    dispatch_status: FRACTURE_DISPATCH_NOT_ALLOWED
    runtime_physical_dispatch_enabled: false
    buz29_execution_allowed: false

case:
  id: diagnostic_missing_sigmatheta_blocks

lot:
  fracture:
    fracture_model: PKN
    fracture_gate_diagnostics:
      enabled: true
      mode: pre_runner
      dispatch_runtime_enabled: false
