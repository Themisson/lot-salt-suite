metadata:
  name: modern_json_volume_balance_pre_iterative
apb_lot:
  output_format: json
  legacy_dat_output_enabled: true
  leakoff_coupling_mode: volume_balance
  salt_displacement_mode: pre_iterative
caveat: "Diagnostic fixture only; not a physical APB validation."
